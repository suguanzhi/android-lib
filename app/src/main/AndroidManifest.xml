<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sgz.androidlib">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".MyApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavigationActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestLoginActivity" />
        <activity android:name=".TestTakePhotoActivity" />
        <activity android:name=".TestTakePhotosActivity" />
        <activity android:name=".TestScrollerLayoutActivity" />
        <activity android:name=".TestWebLayoutActivity" />
        <activity android:name=".TestWebActivity" />
        <activity android:name=".TestDatePickActivity" />
        <activity android:name=".TestViewActivity" />
        <activity android:name=".TestActivityActivity" />
        <activity android:name=".TestDialogActivity" />
        <activity android:name=".TestFragmentActivity" />
        <activity android:name="com.android.sgzcommon.activity.QRCodeActivity" />
        <activity android:name="com.android.sgzcommon.activity.BarCodeActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.sgz.androidlib"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>